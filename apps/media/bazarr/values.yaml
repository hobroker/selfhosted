defaultPodOptions:
  automountServiceAccountToken: false

controllers:
  main:
    containers:
      main:
        image:
          repository: linuxserver/bazarr
          tag: 1.5.6
          pullPolicy: IfNotPresent

service:
  main:
    controller: main
    type: ClusterIP
    ports:
      http:
        port: 6767

persistence:
  config:
    existingClaim: bazarr-config-pvc
    globalMounts:
      - path: /config
  nebula:
    existingClaim: bazarr-nebula-pvc
    globalMounts:
      - path: /mnt/nebula

ingress:
  main:
    enabled: true
    annotations:
      traefik.ingress.kubernetes.io/router.entrypoints: websecure
    hosts:
      - host: bazarr.hobroker.me
        paths:
          - path: /
            pathType: Prefix
            service:
              identifier: main
