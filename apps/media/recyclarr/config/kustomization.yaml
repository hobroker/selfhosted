resources:
  - pv.yaml
  - infisical-recyclarr-secret.yaml
  - job.yaml
configMapGenerator:
  - name: recyclarr-config
    namespace: default
    options:
      disableNameSuffixHash: true
    files:
      - recyclarr.yml=recyclarr.yml
