repositories:
  - name: minio
    url: https://charts.min.io/

releases:
  - name: minio
    version: 5.1.0
    atomic: true
    namespace: kube-system
    chart: minio/minio
    values:
      - values.yaml
