repositories:
  - name: grafana
    url: https://grafana.github.io/helm-charts

releases:
  - name: loki
    version: 2.10.1
    atomic: true
    namespace: monitoring
    chart: grafana/loki-stack
    values:
      - values.yaml
