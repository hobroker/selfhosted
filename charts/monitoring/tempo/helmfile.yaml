repositories:
  - name: grafana
    url: https://grafana.github.io/helm-charts

releases:
  - name: tempo
    version: 1.25.1
    atomic: true
    namespace: monitoring
    chart: grafana/tempo-distributed
    values:
      - values.yaml
