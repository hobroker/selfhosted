repositories:
  - name: grafana
    url: https://grafana.github.io/helm-charts

releases:
  - name: tempo
    version: 1.9.4
    atomic: true
    namespace: monitoring
    chart: grafana/tempo-distributed
    values:
      - values.yaml
